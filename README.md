# orkestr

Local Git hooks live in `scripts/git-hooks`.
Enable them in a clone with `git config core.hooksPath scripts/git-hooks`.
 A modern, open-source automation engine for Android - orchestrate triggers, conditions, and actions with ease.
